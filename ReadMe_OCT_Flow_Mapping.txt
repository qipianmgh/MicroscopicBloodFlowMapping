1.	use the matlab command:
	addpath(genpath(PATH))
	where PATH = full directory to the gui and supporting script folder. In may case for example, I would use addpath(genpath('C:\Users\alfad\Documents\Assets'));

2.	call the the software directly from matlab command window:
	OCT_Flow_Mapping